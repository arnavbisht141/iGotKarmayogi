# Onboarding feature

Owns the learner setup wizard, its local form state, navigation, and API submission. It consumes global authentication and shared UI primitives. `app/onboarding/page.tsx` only maps the URL to the feature component.

Future folders: add `steps/` when individual steps are independently maintained.
