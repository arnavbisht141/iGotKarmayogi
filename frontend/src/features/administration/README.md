# Administration feature

Owns the administrative dashboard, moderation/form state, and its API interactions. It consumes the global auth provider and shared UI. `app/admin/page.tsx` is only the stable route entry.

Future folders: add `users/` or `courses/` only when there are separate, maintained admin subdomains.
