# Dashboard feature

Owns the home dashboard presentation and its client-side course/progress data loading. It consumes auth and i18n providers plus the shared API/types layer. The `/home` route remains a thin entry point.

Future folders: add `components/sections` only when dashboard sections are reused or independently complex.
