# Profile feature

Owns profile viewing and editing state, profile API calls, and the `/profile` route implementation. It consumes global auth/i18n and shared UI rather than owning a parallel identity store.

Future folders: add `forms/` only if profile form sections become reusable.
