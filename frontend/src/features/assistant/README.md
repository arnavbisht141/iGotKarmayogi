# Assistant feature

Owns the AI assistant widget and its chat request state. The root layout renders it as application-wide feature UI; it continues to consume shared UI, i18n, and the API client.

Future folders: add `conversation/` only if the widget is decomposed into reusable transcript and composer modules.
