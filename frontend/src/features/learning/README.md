# Learning feature

Owns the interactive course player, lesson state, course route/query parsing, progress updates, and completion feedback. It uses the shared API/types/i18n layers and its route remains `/learn/[courseId]`.

Future folders: add `player/` when the player is split into independently maintained controls or panels.
