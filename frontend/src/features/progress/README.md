# Progress feature

Owns enrolled-learning and certificate-history UI. Both `/my-learning` and `/progress` deliberately render the same feature component to preserve current behavior. It consumes auth, i18n, API/types, and the shared certificate modal.

Future folders: add `components/` beneath this feature only for extracted, reused progress views.
