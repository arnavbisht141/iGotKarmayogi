# Assessments feature

Owns assessment delivery, answer state, submission, result feedback, and the `/assess/[assessmentId]` route implementation. Certificate presentation remains in `components/certificate` because it is used by both assessment and progress flows.

Future folders: add `question-types/` when more than one concrete question renderer exists.
