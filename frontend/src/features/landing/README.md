# Landing feature

Owns the public entry landing experience and its local carousel/navigation state. `app/page.tsx` remains the stable `/` route entry point.

Future folders: add `sections/` only when landing sections become independently maintained.
